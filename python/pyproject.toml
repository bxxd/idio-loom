[project]
name = "idio-loom"
version = "0.2.0"
description = "Python wrapper for the loom multi-agent turn engine"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["idio_loom*"]
